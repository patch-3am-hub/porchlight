node_modules/
dist/
dist-single/
supabase/.temp/
.env
.env.local
*.log
# .env.production is committed on purpose: it holds only public client config
# (Supabase URL + anon key). Both are shipped to every browser anyway; RLS guards the data.
